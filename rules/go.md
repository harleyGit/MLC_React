# go.md

## Go
- 遵循 `gofmt` 和 Go 官方命名规范
- 保持现有目录结构、package 划分、接口定义稳定
- 优先简单直接实现，不擅自引入复杂设计

## 错误
- 必须处理 error
- 不忽略 err
- 不擅改错误语义

## 检查
- 有条件优先执行 `gofmt`
- 有条件优先执行 `go vet ./...`
- 有条件优先执行 `go build ./...`
